age = 20
name = "Victor"

print(type(age), age.bit_length()) #The bit_length is a method and is specific to integers
print(type(name), len(name)) #The len is a function and is used for many data types including strings, that's why we pass the string as an argument to the function.
